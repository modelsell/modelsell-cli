{
  "name": "modelsell-cli",
  "version": "0.1.0",
  "description": "Configure Codex, Claude Code, and Gemini CLI to use ModelSell.",
  "type": "module",
  "bin": {
    "modelsell": "./bin/modelsell.js"
  },
  "scripts": {
    "build": "node scripts/build-binaries.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@iarna/toml": "^2.2.5"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.19.0",
    "esbuild": "^0.28.0"
  }
}
